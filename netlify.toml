[build]
  command = "node scripts/build-content.js"
  publish = "."

# Evita que Netlify intente procesar/optimizar el admin de Decap CMS
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Robots-Tag = "noindex"
